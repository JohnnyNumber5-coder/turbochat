<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Turbo Chat</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "About", pages_about_path, class: "nav-link" %>
        </li>
        <% if current_user %>
        <li class="nav-item">
          <%= link_to "Start Chatting", rooms_path, class: "nav-link" %>
        <% end %>  
      </ul>
      <% if current_user %>
      <%= button_to 'Edit Account', edit_user_registration_path, method: :get %>
      <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: "nav-item" %>
      <% else %>
      <%= button_to 'Login', new_user_session_path %>
      <%= button_to "Sign Up", new_user_registration_path, method: :get %>
      <% end %>
    </div>
  </div>
</nav>